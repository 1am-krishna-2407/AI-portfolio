# Knowledge Base

Place verified documents in this folder to extend Krishna’s portfolio knowledge base.

Supported file types:
- Markdown (`.md`)
- Plain text (`.txt`)
- PDF (`.pdf`)
- Word document (`.docx`)

Docs are indexed automatically when you run `python scripts/build_index.py`.
